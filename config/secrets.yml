# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.
shared:
  twilio_key: AC592875f15c8aa07728f754d8efd086e1
  twilio_secret: 232fb9afaf465642c0079920bfebbc3e

development:
  secret_key_base: 27bf6ca9f0bb17de679aa78a31469edfd749b9a561b93c1fc721106e54bfa4f540e22184e1bc39442f31535339e70b0f9bd13cfde0ac1f408aea9e75525eb8c8

test:
  secret_key_base: bbd1bea3cd4d9b837acd28a716bf9f0dde8d56411174477bb66c56c027fb0bc78c799e90b60f168d633f176c813411bed7c499f726858f90e7c04c0f9cf04844

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
